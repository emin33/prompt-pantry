---
const currentPath = Astro.url.pathname;

const navLinks = [
  { href: "/recipes", label: "Recipes" },
  { href: "/techniques", label: "Techniques" },
  { href: "/about", label: "About" },
];
---

<header class="border-b border-warm-gray/15 bg-cream/80 backdrop-blur-sm sticky top-0 z-50">
  <nav class="max-w-5xl mx-auto px-4 sm:px-6 h-16 flex items-center justify-between">
    <a href="/" class="font-display text-2xl text-charcoal hover:text-terracotta transition-colors no-underline">
      Prompt Pantry
    </a>
    <ul class="flex items-center gap-6 text-sm font-medium">
      {navLinks.map(({ href, label }) => (
        <li>
          <a
            href={href}
            class:list={[
              "no-underline transition-colors",
              currentPath.startsWith(href)
                ? "text-terracotta"
                : "text-warm-gray hover:text-charcoal",
            ]}
          >
            {label}
          </a>
        </li>
      ))}
    </ul>
  </nav>
</header>
